#include "stack.h"
i

void	stackinit(s_stack *stack, int size)
{
	stack = malloc(sizeof(s_stack));
	stack->size = size;
	
}

void	init(void)
{
	return ;
}

int main(void)
{
	// this thing is not going to work if the parser explodes
	
	if ()
	
	// testcasesl
	return (0);
}
